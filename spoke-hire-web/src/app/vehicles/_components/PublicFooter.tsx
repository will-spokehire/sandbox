/**
 * Public Footer
 * 
 * Re-exports the Footer wrapper component.
 * This maintains backward compatibility with existing imports.
 * For server components, use FooterWrapper directly.
 * For client components, use FooterClient.
 */
export { FooterWrapper as PublicFooter } from '~/components/footer/FooterWrapper'

