# ============================================
# Preview Environment Configuration Template
# ============================================
# Copy this file to .env.preview and fill in your values
# Load with: export $(cat .env.preview | xargs)
#
# Uses standard Next.js/Supabase environment variable names

# ============================================
# Supabase Database
# ============================================

# Direct Connection URL (for migrations and schema changes)
# Port: 5432 (direct connection, NOT pooled)
# Get from: Supabase Dashboard → Settings → Database → Connection string
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# Connection Pooler URL (for application queries)
# Port: 6543 (pooled connection with PgBouncer)
# Get from: Supabase Dashboard → Settings → Database → Connection pooling
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# ============================================
# Supabase Auth & Storage
# ============================================

# Project URL (standard Next.js public env var)
# Get from: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"

# Anonymous (public) key - safe for client-side use (standard Next.js public env var)
# Get from: Supabase Dashboard → Settings → API → Project API keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Service role key - SERVER-SIDE ONLY, never expose to client
# Get from: Supabase Dashboard → Settings → API → Project API keys → service_role
# ⚠️  KEEP THIS SECRET!
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# ============================================
# Application Configuration
# ============================================

# Application base URL - used for generating links in emails and redirects
# OPTIONAL: Auto-detects Vercel preview URL if not set
# Only set this if you want to override the automatic Vercel URL detection
# Example: "https://your-project-git-preview-yourteam.vercel.app"
# NEXT_PUBLIC_APP_URL="https://your-custom-preview-url.vercel.app"

# ============================================
# Environment & Debug Settings
# ============================================

# Node environment
NODE_ENV="production"

# Email Debug Mode - logs emails to console instead of sending
# Set to "true" to prevent sending real emails in preview
EMAIL_DEBUG="true"

# Test Email Override - redirect all emails to a test address
# When set, all emails will be sent to this address instead of real users
# Useful for testing without spamming real users
TEST_EMAIL_OVERRIDE="preview-test@example.com"

# ============================================
# Optional: Email Service (Loops)
# ============================================

# Loops API Key for sending transactional emails
# Get from: https://app.loops.so/settings?page=api
# Can use same key as production or separate preview key
LOOPS_API_KEY=""

# ============================================
# Optional: AI Generation (Google Gemini)
# ============================================

# Google Generative AI API Key for AI-powered vehicle descriptions
# Get from: https://aistudio.google.com/app/apikey
# Can use same key as production
GOOGLE_GENERATIVE_AI_API_KEY=""

# ============================================
# Notes
# ============================================

# How to get Supabase credentials:
# 1. Create new Supabase project (https://supabase.com/dashboard)
# 2. Name it "spokehire-preview"
# 3. Choose same region as production
# 4. Go to Settings → Database for connection strings
# 5. Go to Settings → API for keys
# 6. Replace [PROJECT-REF], [PASSWORD], [REGION] with actual values

# Security Notes:
# - Never commit this file to version control
# - Keep SERVICE_ROLE_KEY secret (server-side only)
# - Use separate credentials from production
# - Enable email debug mode to prevent real emails

# Database Setup:
# 1. Load this file: export $(cat .env.preview | xargs)
# 2. Run setup: npm run db:setup
# 3. Verify: npm run db:verify
# 4. Migrate data: (coming soon)

# ============================================

